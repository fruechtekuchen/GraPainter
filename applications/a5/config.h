#pragma once


namespace config {
    namespace player {
        extern float max_speed;
        extern float acceleration;
        extern float deceleration;
        extern float max_rotation_speed;
        extern float rotation_acceleration;
        extern float rotation_deceleration;
    }
}